import type { Supermarket } from "@meal-pilot/core";

/** Etiquetas en español para el enum `supermarket` (ver ADR-0023) — compartidas entre el formulario de ingrediente y su ficha de catálogo. */
export const SUPERMARKET_LABELS: Record<Supermarket, string> = {
  mercadona: "Mercadona",
};
