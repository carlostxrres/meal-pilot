# Secrets — nunca deben llegar al repo
.env
.env.*
!.env.example
supabase-access-token

# Node
node_modules/
dist/

# TypeScript — caché incremental de tsc, se regenera solo
*.tsbuildinfo

# Next.js
.next/
next-env.d.ts
