{
  "name": "comida-diaria",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "generate": "npm run generate -w @comida-diaria/cli",
    "test": "npm test -w @comida-diaria/core",
    "gen:types": "npm run gen:types -w @comida-diaria/core",
    "dev:web": "npm run dev -w @comida-diaria/web"
  }
}
